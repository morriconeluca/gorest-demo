import button from './button.styles';

const circle = {
  primary: `${button.primary} rounded-full! w-12! px-0!`,
  secondary: `${button.secondary} rounded-full! w-12! px-0!`,
  tertiary: `${button.tertiary} rounded-full! w-12! px-0!`,
};

export default circle;
